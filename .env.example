# Kafka Configuration
# Copy this file to .env and fill in your actual values

# Required: Your Kafka broker URLs (comma-separated if multiple)
KAFKA_BROKERS=your-broker-url:9092

# Required for most cloud Kafka services
KAFKA_USERNAME=your-username-or-api-key
KAFKA_PASSWORD=your-password-or-api-secret

# SSL Configuration (usually 'true' for cloud services)
KAFKA_SSL=true

# Topic name (will be created if it doesn't exist)
KAFKA_TOPIC=click-events

# Client ID (optional)
KAFKA_CLIENT_ID=portfolio-tracker

# Examples for different Kafka services:

# Confluent Cloud:
# KAFKA_BROKERS=pkc-xxxxx.us-west-2.aws.confluent.cloud:9092
# KAFKA_USERNAME=your-confluent-api-key
# KAFKA_PASSWORD=your-confluent-api-secret
# KAFKA_SSL=true

# AWS MSK with IAM:
# KAFKA_BROKERS=b-1.your-cluster.xxxxx.c2.kafka.us-west-2.amazonaws.com:9092
# KAFKA_SSL=true
# (No username/password needed for IAM auth)

# Self-hosted Kafka:
# KAFKA_BROKERS=your-server:9092
# KAFKA_SSL=false
# (Username/password optional depending on your setup)
